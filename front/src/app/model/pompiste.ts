export class Pompiste {
    idGess ? : string;
    name ? : string;
    dateNaissance ? : Date;
    CIN ? : number;
    dateCIN ? : Date;
    payement ? : string;
    famille ? : string;
    travail ? : string;
    address ? : string;
    tel ? : string;
    email ? : string;
    motpass ? : string;
    contrat ? : string;
    type ? : number;
    active ? : number;
}
  